#include "main.h"
#include<stdio.h>
#include<stdlib.h>

/**
 * print_alphabet - Prints the alphabet in lowercase followed by a new line.
 *
 * Return: Always 0.
 */
void print_alphabet(void)

{
	int lower = 97

	while (lower < 123)
		_putchar(lower);
		lower++
	_putchar(\n);

return (0);
}
